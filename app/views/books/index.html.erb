<h1>Books#index</h1>

<h2>Books</h2>
<table>
  <thead>
    <tr>
      <th class="index-section-heading">Title</th>
      <th class="index-section-heading">Opinion</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= link_to book.title, book_path(book) %></td>
        <td><%= book.body %></td>
      </tr>
    <% end %>
  </tbody>
</table>